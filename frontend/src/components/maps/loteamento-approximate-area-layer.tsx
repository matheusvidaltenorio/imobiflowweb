/**
 * Camada de área aproximada: use `LoteamentoAreaLayer` com features `areaStyle: 'APROXIMADA'`
 * (polígono cadastrado ou círculo de fallback). Mantido como módulo de responsabilidade explícita.
 */
export { LoteamentoAreaLayer as LoteamentoApproximateAreaLayer } from '@/components/maps/loteamento-area-layer';
