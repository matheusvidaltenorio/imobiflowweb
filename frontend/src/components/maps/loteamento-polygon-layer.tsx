/**
 * @deprecated Prefer `LoteamentoAreaLayer`, que aplica estilos por precisão (EXATA / APROXIMADA / PENDENTE).
 */
export { LoteamentoAreaLayer as LoteamentoPolygonLayer } from '@/components/maps/loteamento-area-layer';
