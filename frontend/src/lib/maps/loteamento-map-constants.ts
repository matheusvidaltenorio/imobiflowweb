/** Raio do círculo de fallback quando há ponto (lat/lng) mas não há polígono — EXATA (área menor). */
export const DEV_FALLBACK_RADIUS_EXACT_M = 100;

/** Raio do fallback visual para localização APROXIMADA (área maior). */
export const DEV_FALLBACK_RADIUS_APPROX_M = 450;
